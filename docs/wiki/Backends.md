# Backends

This page mirrors `docs/public/backends.md`.

It explains when to use `rmbg`, `imgly`, `withoutbg`, MLX, ComfyUI, Draw Things, and llama.cpp, plus the `input_mode`, `input_image`, and `reference_image` flows.
