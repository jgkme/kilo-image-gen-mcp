# Configuration

This page mirrors `docs/public/configuration.md`.

Use it for env vars, Kilo client examples, local MLX examples, the shared withoutBG daemon settings, and the Streamable HTTP transport option.
