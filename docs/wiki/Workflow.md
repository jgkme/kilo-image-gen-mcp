# Workflow

This page mirrors `docs/public/workflow.md`.

Use it to understand the full generate -> cleanup -> inspect -> optimize flow, including local MLX and OpenAI-compatible runs, async polling, batch variants, and reference-image editing.
