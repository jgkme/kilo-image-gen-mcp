{
  "name": "img-gen-mcp",
  "version": "0.10.2",
  "description": "Local-first MCP server for image generation, editing, async jobs, workflow tracking, image analysis, local bridges, Streamable HTTP, and web-ready finalization.",
  "type": "module",
  "bin": {
    "img-gen-mcp": "server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jgkme/img-gen-mcp.git"
  },
  "license": "MIT",
  "files": [
    "server.js",
    "scripts/",
    "withoutbg-daemon/",
    "skills/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "start": "node ./server.js",
    "serve:http": "IMAGE_MCP_TRANSPORT=http node ./server.js",
    "bootstrap:local": "node ./scripts/bootstrap-local-runtime.mjs",
    "smoke": "node ./scripts/smoke-image.mjs",
    "test:workflow": "node ./scripts/test-workflow-loop.mjs",
    "test:http": "node ./scripts/http-smoke.mjs",
    "smoke:openrouter": "node ./scripts/smoke-image.mjs --provider openrouter --prompt 'a colorful parrot perched on a branch, simple background'",
    "smoke:openai": "node ./scripts/smoke-image.mjs --provider openai --prompt 'a red panda wearing glasses, clean studio background' --purpose portrait",
    "smoke:local": "node ./scripts/smoke-image.mjs --provider openai-compatible --local-provider openai-compatible --prompt 'a crisp neon logo mark on dark background'",
    "smoke:bg": "node ./scripts/smoke-image.mjs --tool background_remove --input-image generated-images/parrot.png --model modnet",
    "demo:bg": "node ./scripts/smoke-image.mjs --tool background_remove --input-image generated-images/j-gemini-openrouter.png --output generated-images/demo-bg.png",
    "demo:finalize": "node ./scripts/smoke-image.mjs --tool finalize_image --input-image generated-images/j-gemini-openrouter.png --remove-background true --background-backend imgly --background-model medium --trim true --output generated-images/demo-finalize.png"
  },
  "dependencies": {
    "@imgly/background-removal-node": "^1.4.5",
    "@modelcontextprotocol/sdk": "^1.17.2",
    "axios": "^1.7.9",
    "form-data": "^4.0.3",
    "rmbg": "^0.1.0",
    "sharp": "^0.33.5"
  }
}
